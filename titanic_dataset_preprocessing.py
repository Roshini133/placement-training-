# -*- coding: utf-8 -*-
"""Titanic_dataset_preprocessing.ipynb

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/1UC5Yg4JEip2T19Cey6lR-lF5puVEG98P
"""

import os
os.environ['KAGGLE_USERNAME'] = 'netflix'
os.environ['KAGGLE_KEY'] = 'KGAT_9a962221b2ac769b0fb2fb3cfb6b185a'

!kaggle datasets download -d 'yasserh/titanic-dataset'

!unzip /content/titanic-dataset.zip -d /content/titanic

import pandas as pd
df = pd.read_csv("/content/titanic-dataset.zip")
df.head(10)

df.tail(10)

print(len(df))

print(df.shape)

df.info()

df.describe()

df.duplicated().sum()

df.dtypes

print(df.columns)

print(df['Survived'])

print(df['Age'].median())

df.isnull().sum()

df['Age'].fillna(df['Age'].median(), inplace=True)

print(df[['Age']].head(10))

print(df['Age'].isnull().sum())

df['Embarked'].fillna(df['Embarked'].mode()[0], inplace=True)

print(df['Embarked'].head(10))
print(df['Embarked'].isnull().sum())

df['HasCabin'] = df['Cabin'].notnull().astype(int)
df.drop('Cabin', axis=1, inplace=True)

df.drop_duplicates(inplace=True)
print(df)

df['Sex'] = df['Sex'].map({'male':0, 'female':1})

df['FamilySize'] = df['SibSp'] + df['Parch'] + 1
print(df['FamilySize'])

df.drop(['Ticket', 'PassengerId'], axis=1, inplace=True)

df.head()
df.info()

df.isnull().sum()

import matplotlib.pyplot as plt
plt.boxplot(df['Pclass'])
plt.title('Outlier Detection using Boxplot (pclass)')
plt.ylabel('Fare')
plt.show()

plt.scatter(df.index, df['FamilySize'], color='red')
plt.xlabel('Index (formerly PassengerId)')
plt.ylabel('FamilySize')
plt.title('Index vs FamilySize')
plt.show()

df = pd.get_dummies(df, columns=['Embarked'], drop_first=True)

X = df.drop(['Survived', 'Name'], axis=1)
y = df['Survived']

from sklearn.model_selection import train_test_split
from sklearn.linear_model import LogisticRegression
from sklearn.metrics import accuracy_score, confusion_matrix, classification_report

X_train, X_test, y_train, y_test = train_test_split(X, y, test_size=0.3, random_state=42)

model = LogisticRegression(max_iter=1000)
model.fit(X_train, y_train)
print("Model Trained")

y_pred = model.predict(X_test)

accuracy = accuracy_score(y_test, y_pred)
cm = confusion_matrix(y_test, y_pred)
report = classification_report(y_test, y_pred)

print(f"Accuracy: {accuracy:.4f}")
print("Confusion Matrix:")
print(cm)
print("Classification Report:")
print(report)